export interface User { id: string; email: string; }
export interface Workflow { id: string; name: string; steps: any[]; }
export interface PromptLog { id: string; prompt: string; response: string; }
